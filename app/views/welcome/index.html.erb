<!DOCTYPE html>
<html>
<head>
<body>

<h1> Welcome to the 2016 Football Frenzy Football League</h1>

<h2> The Final Rankings of the 2016 season</h2>

<ul class="nav nav-tabs " role="tablist">
  <% for i in 1...11 %>
    <li><%= link_to @teams.find(i).name[0...11] + '..', '/teams/' + i.to_s + '/' %></li>
  <% end %>
</ul>

<table class="table table-striped table-bordered finalRankings">
    <thead>
      <tr>
      	<td>Team</td>
      	<td>Rank</td>
      </tr>
    </thead>
    <tbody>
      <% @teams.to_a.each do |team| %>
        <% if team.teamNumber != 0 %>
          <tr>
            <td><%= team.name %></td>
            <td><%= team.finalRank  %></td>
          </tr>
        <% end %>
      <% end %>
    </tbody>
 </table>
</body>
</html>